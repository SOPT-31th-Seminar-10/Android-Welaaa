<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="bookDescription"
            type="com.sopt.welaaa.data.model.BookDescription" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="20dp">

        <ImageView
            android:id="@+id/iv_item_description"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_description_dummy"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/tv_item_description"
            style="@style/TextAppearance.Welaaa.System4.Medium"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="10dp"
            android:layout_marginBottom="10dp"
            android:background="@drawable/background_gray_r23"
            android:paddingHorizontal="10dp"
            android:paddingVertical="4dp"
            android:text="@string/item_description_sample"
            android:textColor="@color/welaaa_grey_100"
            app:layout_constraintBottom_toBottomOf="@id/iv_item_description"
            app:layout_constraintEnd_toEndOf="@id/iv_item_description" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>